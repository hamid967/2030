import SwiftUI

/// Brand anchors shared by every regional theme.
enum WarifBrand {
    static let berry = Color(hex: "#895B75")
    static let berryStrong = Color(hex: "#6F405B")
    static let ivory = Color(hex: "#FFF9F6")
    static let textPlum = Color(hex: "#30272D")
    static let rose = Color(hex: "#C98D9E")
    static let surface = Color(hex: "#FFFFFF")
    static let border = Color(hex: "#E8DDE1")
    static let mutedText = Color(hex: "#6B6268")
    static let sage = Color(hex: "#3E6B57")
    static let alert = Color(hex: "#B65343")

    static let cardCornerRadius: CGFloat = 8
    static let controlCornerRadius: CGFloat = 10
    static let minTouchTarget: CGFloat = 44
}
